// if we hac t do it int logn
// int left = 0;
// int right = nums.length - 1;

// while (left <= right) {
//     int mid = left + (right - left) / 2

// always find mid by int mid = left + (right - left) / 2 